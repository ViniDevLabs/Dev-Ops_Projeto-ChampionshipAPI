spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/championship-api
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update # validate | update | create | create-drop
    properties:
      hibernate:
       "[format_sql]": true